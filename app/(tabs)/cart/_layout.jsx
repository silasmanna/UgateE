import { Stack } from "expo-router";

const Notelayout = () => {
  return (
    <Stack
      screenOptions={{
        name: "item",
        headerShown: false,
      }}
    />
  );
};

export default Notelayout;
